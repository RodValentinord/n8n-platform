# ATENÇÃO: Este arquivo foi migrado para o Helm chart.
#
# A AnalysisTemplate "n8n-canary-analysis" agora é gerenciada em:
#   charts/n8n/templates/analysis-template.yaml
#
# Este arquivo é mantido apenas como referência histórica.
# NÃO aplicar manualmente via kubectl — o helm upgrade já gerencia o ciclo de vida.
#
# ── Migração one-time (já executada) ─────────────────────────────────────────
# Quando um recurso existe no cluster criado via kubectl apply e você quer passar
# a gerenciá-lo pelo Helm, é necessário adicionar as labels/annotations de ownership
# antes do primeiro helm upgrade que inclui o recurso:
#
#   kubectl annotate analysistemplate n8n-canary-analysis -n n8n \
#     meta.helm.sh/release-name=n8n \
#     meta.helm.sh/release-namespace=n8n \
#     --overwrite
#
#   kubectl label analysistemplate n8n-canary-analysis -n n8n \
#     app.kubernetes.io/managed-by=Helm \
#     --overwrite
#
# Sem isso, o Helm recusa o upgrade com "cannot be imported into the current release".
# ─────────────────────────────────────────────────────────────────────────────
